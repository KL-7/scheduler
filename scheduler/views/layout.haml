!!!
%html
  = haml :head

  %body
    %div.content
      - if current_user
        %div.user-block
          = current_user.name
          - if current_user.role
            = "(#{current_user.role})"
          %a{ href: "/logout" } Logout

      = haml :flash

      %div.container.well
        - if current_user && !navigation.empty?
          %ul.pills
            - navigation.each do |item|
              %li{ class: item[:active] ? 'active' : '' }
                %a{ href: item[:path] }= item[:title]

        = yield
