%form.well{ action: '/a/subject', method: 'post' }
  %div.clearfix.inline-input
    %label{ for: 'name' } Name:
    %div.input
      %input{ name: "name", type: "text", value: params['name'] }/

  %input.btn.primary{ type: "submit", value: "Create" }/

- unless @subjects.empty?
  %table.bordered-table
    %thead
      %th #
      %th{ width: 700 } Name
      %th{ width: 100 } Actions
    %tbody
    - @subjects.each_with_index do |s, i|
      %tr
        %th= i + 1
        %td= s.name.empty? ? '&mdash;' : s.name
        %td
          %button.btn.delete-record{ 'data-record-id' => s.id, 'data-delete-path' => '/a/subject' } Delete

- else
  No subjects.
